import { FieldProps } from "formik";
export declare function SwitchControl({ field, form, label, tooltip, disabled, size }: FieldProps & {
    label: string;
    tooltip?: string;
    disabled?: boolean;
    size: "small" | "medium";
}): import("@emotion/react/jsx-runtime").JSX.Element;
